import styled from 'styled-components';

export const OpenBagButton = styled.button`
    position: relative;
    background: transparent;
    border: none;
    padding: 0;
    margin: 0;
    width: 24px;
    cursor: pointer;

    &:focus,
    :active {
        outline: 0;
    }
`;
